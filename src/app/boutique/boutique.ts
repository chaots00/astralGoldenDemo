import { NodeCompatibleEventEmitter } from "rxjs/internal/observable/fromEvent";

export interface Boutique {
    id:string;
    nom : string;
    dega:number;
    prix_vente:number;
    prix_achat:number;
    nombre_poseder:number;
}
